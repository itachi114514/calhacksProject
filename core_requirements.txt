websockets==15.0.1
numpy==1.26.4
webrtcvad==2.0.10
sounddevice==0.5.1
funasr==1.2.6
opencv-python==4.11.0.86
mediapipe==0.10.21
face-recognition==1.3.0
openai==1.74.0
aiohttp==3.11.16
python-dotenv == 1.1.0
torch == 2.5.1
torchaudio == 2.5.1

# gpt_sovits requirements
lightning #
matplotlib
ffmpeg-python
opencc
onnxruntime-gpu
einops
gradio
cn2an
pypinyin
jieba_fast
LangSegment == 0.3.5 # There will be an error say 0.3.5 not found, update gpt_sovits or install 0.3.5 manually to fix it
#end of gpt_sovits requirements

# for faster whisper only
faster_whisper

# for gpt_sovits in English
g2p_en
wordsegment

#To fix dlib on Mac M series: conda install -c conda-forge dlib
#To fix dlib "ERROR: Failed to build installable wheels for some pyproject.toml based projects (dlib)": conda install -c conda-forge dlib